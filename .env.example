# Every var below is REQUIRED (the app throws at first use if one is unset),
# EXCEPT the three feature-flags in the last section, which turn a feature off
# when left blank. There are no code defaults: configuration must be explicit.

# ---- Email (Resend) - reservation confirmation + cancellation emails ----
# Verified Resend sender (or onboarding@resend.dev while your domain verifies).
EMAIL_FROM=onboarding@resend.dev
# Public base URL, used to build links in emails (e.g. the invite/activation
# link) and the email logo (served from the app at APP_BASE_URL/logo.svg).
APP_BASE_URL=https://scheduler.example.com

# ---- Admin login (env credentials; the only account there is) ----
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=change-me
# Long random string signing the admin session cookie and the emailed cancel links.
AUTH_SECRET=replace-with-a-long-random-string

# ---- Login brute-force protection ----
# Two independent buckets guard the login (see src/lib/rate-limit.ts). Because the
# whole coworking space shares one public IP, the per-account bucket is strict but
# never permanently bans (an attacker must not lock the admin out forever), while
# the per-IP bucket is lenient and only bans under sustained abuse.
# Per-account: failed attempts before an (escalating) lockout.
LOGIN_MAX_ATTEMPTS=5
# Per-account & per-IP base lockout seconds; escalates ×N per lockout.
LOGIN_BLOCK_SECONDS=60
# Per-IP: failed attempts (across any accounts) before an IP lockout.
LOGIN_IP_MAX_ATTEMPTS=20
# Per-IP base lockout seconds; escalates ×N per lockout.
LOGIN_IP_BLOCK_SECONDS=60
# Per-IP lockouts before the IP is banned outright (until restart).
LOGIN_MAX_LOCKOUTS=10
# The public booking form reuses LOGIN_IP_MAX_ATTEMPTS / LOGIN_IP_BLOCK_SECONDS as
# its own per-IP throttle (never banning): that many bookings per IP, then a pause.

# ---- Cloudflare Turnstile (optional bot protection on the booking form) ----
# Both keys or neither: with either unset the check is skipped entirely, which is
# how dev and the test suite run. Create a widget at
# Cloudflare dashboard > Turnstile > Add widget (hostname = your domain).
# The site key is public (rendered into the page); the secret key never is.
# TURNSTILE_SITE_KEY=0x4AAAAAAA...
# TURNSTILE_SECRET_KEY=0x4AAAAAAA...

# ---- Trusted proxy proof (optional; makes the rate limiter unspoofable) ----
# cf-connecting-ip / x-forwarded-for are whatever the caller typed. Cloudflare
# overwrites them, but a request straight to the Fly origin does not go through
# Cloudflare, so rotating one there hands every attempt a fresh rate-limit bucket.
# Set this to a random string AND add the matching Cloudflare Transform Rule, and
# those headers are then trusted only on a request carrying the proof; anything
# else falls back to fly-client-ip, which the caller cannot choose. Blank = off.
#
#   fly secrets set TRUSTED_PROXY_SECRET=$(openssl rand -hex 32)
#   Cloudflare dashboard > Rules > Transform Rules > Modify Request Header
#     > Set static  x-origin-proof = <the same value>
#
# ORDER MATTERS: add the Cloudflare rule FIRST. With the secret set and the rule
# missing, every visitor falls back to the same Cloudflare edge IP and so shares
# one bucket, which throttles the whole site. `fly secrets unset` reverses it.
TRUSTED_PROXY_SECRET=

# ---- Storage ----
# Path to the SQLite DB file (a persistent volume in production).
DATA_FILE=./data/scheduler.db

# ---- Scheduling window ----
# First reservable hour and closing hour (24h). Reservations are a free start/end range.
OPEN_HOUR=9
CLOSE_HOUR=18
# Times snap to this many minutes (so 09:10 is allowed, 09:07 is not). Divides 60.
TIME_STEP_MINUTES=5
# Shortest allowed reservation, in minutes.
MIN_RESERVATION_MINUTES=15
# How many days ahead (including today) members can reserve.
RESERVATION_WINDOW_DAYS=14
# Longest reservation (hours) that is auto-confirmed. Anything longer is created as
# "pending" and must be approved by the admin from the dashboard.
AUTO_APPROVE_MAX_HOURS=2
# Timezone for the day boundary and "already passed" slot logic.
TZ=Europe/Tirane

# ---- Booths ----
# "id:Name:capacity" comma-separated (capacity optional).
SCHEDULER_BOOTHS=booth-1:Booth 1:2,booth-2:Booth 2:4,booth-3:Booth 3:6

# ---- Business contact details (shown in every email) ----
EMAIL_SIGNOFF_NAME=Your Name
BUSINESS_NAME=Innospace Tirana
BUSINESS_PHONE=+000 000 000
BUSINESS_EMAIL=hello@example.com
# Business website: shown as the footer link in emails.
BUSINESS_WEBSITE_URL=https://example.com

# ---- Optional feature-flags (leave blank to turn the feature OFF) ----
# Resend API key. Blank = emails are skipped (dev without sending).
RESEND_API_KEY=
# CORS allowlist, comma-separated origins. Blank = allow any ("*").
ALLOWED_ORIGINS=

# ---- Slack notifications (optional) ----
# Every new booking is announced in one channel. Unset or blank = no posting,
# which is how dev and the test suite run. This URL is a secret: anyone holding
# it can post to the channel, so keep it out of git and out of chat logs.
# Create one at api.slack.com/apps > your app > Incoming Webhooks > Add New
# Webhook to Workspace, then pick the channel. The URL it gives you looks like
# https://hooks.slack.com/services/T0000000/B0000000/xxxxxxxxxxxxxxxxxxxx
#
#   fly secrets set SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
#
# A webhook is bound to the one channel chosen when it was created; to move
# channels, make a new webhook and replace the value.
SLACK_WEBHOOK_URL=
